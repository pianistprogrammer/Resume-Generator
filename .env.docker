# Docker Compose Environment Variables

# MongoDB
MONGODB_ROOT_USERNAME=admin
MONGODB_ROOT_PASSWORD=your-secure-password-here
MONGODB_DB_NAME=jobalert

# Note: Backend service uses ./backend/.env for application secrets
# Make sure backend/.env has all required keys (Anthropic, S3, SMTP, etc.)
