# Docker Compose Environment Variables

# No MongoDB configuration needed - using external MongoDB via MONGODB_URL in backend/.env

# Note: Backend service uses ./backend/.env for all configuration
# Make sure backend/.env has:
# - MONGODB_URL (your MongoDB Atlas/external connection string)
# - All required API keys (Anthropic, S3, SMTP, etc.)
